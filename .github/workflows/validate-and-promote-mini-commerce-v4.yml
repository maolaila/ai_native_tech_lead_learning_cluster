name: Historical workflow (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo '历史初始化/修复工作流已停用。当前学习源码已修正，不得重新生成或自动提升旧模板。'
          exit 1
